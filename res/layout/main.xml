<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <!-- The Layout that contains the non-drawer layouts -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background" >

        <!-- The SwipeLayout that contains the FeedList --> 
        <android.support.v4.widget.SwipeRefreshLayout 
            android:id="@+id/swipe_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent" 
            android:paddingLeft="8dp"
            android:paddingRight="8dp" > 

            <!-- The main list that displays the RSS-feed -->
            <ListView
                android:id="@+id/feed_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:divider="@android:color/transparent"
                android:dividerHeight="8dp" /> 

        </android.support.v4.widget.SwipeRefreshLayout>

        <!-- The fading background -->
        <LinearLayout
            android:id="@+id/fading_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent" 
            android:background="@drawable/bg_fading" />

        <!-- The View that displays the inflated feed-item -->
        <include layout="@layout/popup_view" />

    </FrameLayout>

    <!-- The list that contains the drawer-items -->
    <ListView
        android:id="@+id/drawer_list"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:divider="@android:color/transparent"
        android:choiceMode="singleChoice"
        android:background="@color/background_secondary" />

</android.support.v4.widget.DrawerLayout>
